This study evaluates timing-based architecture fingerprinting under controlled CPU inference conditions using locally executed neural network models. The experiments do not account for distributed cloud inference environments, heterogeneous accelerators, network jitter, dynamic batching, or multi-tenant scheduling effects commonly present in production ML deployment systems. Therefore, the results should be interpreted as a controlled feasibility study of latency-side-channel separability rather than a fully generalized real-world attack.
